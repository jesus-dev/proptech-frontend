export interface Neighborhood {
  id: string;
  name: string;
  cityId: string;
}

export interface City {
  id: string;
  name: string;
  countryId: string;
}

export interface Country {
  id: string;
  name: string;
} 