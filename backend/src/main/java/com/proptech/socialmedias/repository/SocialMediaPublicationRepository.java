package com.proptech.socialmedias.repository;

import com.proptech.socialmedias.entity.SocialMediaPublication;
import io.quarkus.hibernate.orm.panache.PanacheRepository;
import jakarta.enterprise.context.ApplicationScoped;

@ApplicationScoped
public class SocialMediaPublicationRepository implements PanacheRepository<SocialMediaPublication> {
    // Métodos personalizados si se requieren
} 